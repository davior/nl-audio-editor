{
  "id": "dc_remove",
  "version": 1,
  "title": "Remove DC offset",
  "summary": "Remove a constant offset (the mean of each channel), or a slowly drifting one.",
  "category": "gain_level",
  "class": "corrective",
  "scopes": ["clip", "time_range"],
  "params": [
    { "id": "mode", "title": "Mode", "type": "enum", "values": ["mean", "drift"], "default": "mean",
      "help": "mean: subtract each channel's average (as Audacity's Normalize does). drift: subtract a centred moving average." },
    { "id": "window_ms", "title": "Drift window", "type": "number", "unit": "ms", "min": 50, "max": 5000, "step": 10, "default": 1000,
      "help": "Length of the moving average in drift mode." }
  ],
  "identity": [],
  "residual": "exact",
  "measurements": [
    { "id": "offset_removed", "title": "Offset removed (per channel)" }
  ],
  "binding_hints": []
}
